#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long t;
    cin >> t;
    while (t--)
    {
        long long n;
        cin >> n;
        string s;
        cin >> s;

      
        bool b = 0;
        
        if(s[0]==s[n-1]) cout<<"NO"<<endl;
        else cout<<"YES"<<endl;
    }
    return 0;
}