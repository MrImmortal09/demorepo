#include <bits/stdc++.h>
using namespace std;

int main() {
    long long t;
    cin >> t;
    while (t--) {
        long long n;
        cin >> n;

        // set<char> re(s.begin(), s.end());
        // vector<char> vec(re.begin(), re.end());

        for (long long i = 0; i < n; i++) {

            }


        // cout <<  << endl;
    }
    return 0;
}